%h1 TQA Progress

- @tqa_categories.each do |tqa_category|
  %b= tqa_category.name

  %table
    %thead
      %tr
        %th Area
        %th % Run
        %th % Passed
        %th
        %th
        %th

      %tbody
        - tqa_category.tqa_aspects.each do |tqa_aspect|
          %tr
            %td= tqa_aspect.name
            %td= tqa_aspect.percent_run
            %td= tqa_aspect.percent_pass
            %td
              %div{ style: "height:15px;width:300px;border-style:solid;border-width:1px;padding-left:-10px"}
                %div{ style: "float:left;background-color:#5C00B8;height:14px;width:#{tqa_aspect.portion_failed_testing}%;"}
                %div{ style: "float:left;background-color:#8533D6;height:14px;width:#{tqa_aspect.portion_passed_testing}%;"}
            %td= link_to 'Edit', edit_tqa_aspect_path(tqa_aspect)
            %td= link_to 'Destroy', tqa_aspect, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
= link_to 'New TQA Metric', new_tqa_aspect_path
