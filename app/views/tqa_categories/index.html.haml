%h1 TQA Categories

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th

  %tbody
    - @tqa_categories.each do |tqa_category|
      %tr
        %td= tqa_category.name
        %td= link_to 'Edit', edit_tqa_category_path(tqa_category)
        %td= link_to 'Destroy', tqa_category, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Tqa category', new_tqa_category_path
